# tooling

Home for build, release and CI helper scripts. This is where the upstream yarn + yakumo pipeline gets replaced over time (Bun-native build, changesets-style versioning, GitHub Actions workflows).
